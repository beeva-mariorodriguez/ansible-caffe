---
cuda_dependencies:
  - nvidia-cuda-dev
  - nvidia-cuda-toolkit
dependencies:
  - libprotobuf-dev
  - libleveldb-dev
  - libsnappy-dev
  - libhdf5-serial-dev
  - protobuf-compiler
  - libboost-all-dev
  - libgflags-dev
  - libgoogle-glog-dev
  - liblmdb-dev 
  - git
  - build-essential
  - python-skimage
  - python-virtualenv

src_dir: "{{ ansible_env.HOME }}/caffe"
repo_url: "https://github.com/BVLC/caffe"

